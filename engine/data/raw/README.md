# Raw recordings

HTTP response recordings are private raw snapshots and are not final product
data. `discovery-cache/` contains Common Crawl lead caches. Raw snapshots may
contain source descriptions and remain outside the canonical lake.
